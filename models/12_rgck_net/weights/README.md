# M12 weights

Symlink to M10's AdaFace IR-101 (WebFace4M) checkpoint:

```
adaface_ir101_webface4m.pth -> ../../10_adaface_facor/weights/adaface_ir101_webface4m.pth
```

The actual file lives in `models/10_adaface_facor/weights/`. M11 also
uses the same symlink pattern.
